<h2><%= @flat.name %></h2>
<p><%= @flat.description %></p>
<p><%= @flat.address %></p>
<p><%= @flat.number_of_guests %></p>
<p><%= @flat.price_per_night %></p>

<%= link_to 'Destroy', flat_path(@flat), data:{turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
